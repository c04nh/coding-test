N = int(input())
r = 0
arr = []
arr.append(list(map(int, input().split())))

for i in range(1, N):
	arr.append(list(map(int, input().split())))
	r += abs(arr[i][0]-arr[i-1][0])+abs(arr[i][1]-arr[i-1][1])

r += abs(arr[N-1][0]-arr[0][0])+abs(arr[N-1][1]-arr[0][1])
print(r)